import React,{useState} from 'react';import{Link}from'react-router-dom';import{Mail,ArrowLeft}from'lucide-react';import{API_BASE}from'../config';
export default function ForgotPassword(){const[email,setEmail]=useState('');const[message,setMessage]=useState('');const[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);try{const r=await fetch(API_BASE+'/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const d=await r.json();setMessage(d.message||'Unable to send reset email')}catch{setMessage('Server connection failed')}finally{setBusy(false)}};return <div className="min-h-screen bg-slate-950 flex flex-col justify-center py-12 sm:px-6"><div className="sm:mx-auto sm:w-full sm:max-w-md px-4"><h2 className="text-center text-4xl font-extrabold text-slate-100">Forgot your password?</h2><p className="mt-3 text-center text-sm text-slate-500">Enter your email and we’ll send a secure reset link.</p><div className="mt-8 bg-slate-900 py-8 px-4 border border-slate-800 rounded-2xl sm:px-10">{message&&<p className="mb-5 p-4 rounded-xl bg-indigo-500/10 text-indigo-600 text-sm">{message}</p>}<form className="space-y-6" onSubmit={submit}><label className="block text-sm font-medium text-slate-100">Email Address<div className="mt-1.5 relative"><Mail className="absolute left-3 top-3 h-5 w-5 text-slate-500"/><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} className="bg-slate-950/60 block w-full pl-10 pr-3 py-2.5 border border-slate-800 rounded-xl text-slate-100" placeholder="name@company.com"/></div></label><button disabled={busy} className="w-full py-3 rounded-xl font-semibold text-white bg-indigo-600 disabled:opacity-50">{busy?'Sending…':'Send reset link'}</button></form><Link to="/login" className="mt-6 flex justify-center items-center gap-2 text-sm font-semibold text-indigo-600"><ArrowLeft className="w-4 h-4"/>Back to sign in</Link></div></div></div>}
